version: '3.8'
services:
  app:
    build: .
    container_name: finally-app
    image: finally
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env

volumes:
  finally-data:
