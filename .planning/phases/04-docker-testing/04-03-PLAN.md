---
phase: 04-docker-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_main_api_coverage.py
  - backend/tests/test_llm_malformed.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "The existing backend suite passes, including the trade edge-case coverage in test_portfolio.py (insufficient cash, oversell, partial sell, weighted avg cost) — TEST-01 is satisfied by existing tests and this plan asserts/runs them, it does NOT re-create them"
    - "A new full-app integration test exercises GET /api/portfolio, GET /api/portfolio/history, and GET /api/watchlist through the fully-assembled create_app() (closing the TEST-02 gap that test_main.py only covered watchlist)"
    - "A new test covers the valid-JSON-wrong-schema LLM response path (missing message field, wrong-typed message) returning a graceful ChatResponse fallback — closing the TEST-03 gap"
    - "The whole backend suite stays green: cd backend && uv run --extra dev pytest -q exits 0"
  artifacts:
    - "backend/tests/test_main_api_coverage.py — full-app create_app() round-trips for portfolio, history, watchlist (TEST-02 gap-fill)"
    - "backend/tests/test_llm_malformed.py — valid-JSON-wrong-schema graceful fallback (TEST-03 gap-fill)"
  key_links:
    - "Both new test files MUST monkeypatch DB_PATH to a tmp_path file BEFORE create_app()/db is imported (db.get_db_path reads env at call time; constants are import-time) — Pitfall 8"
    - "The full-app test sets STATIC_DIR to a nonexistent path to skip the static mount (matches test_chat_route.py:216-231)"
    - "The malformed-LLM test patches app.llm.completion AFTER setting env and importing app.llm (mirrors test_llm.py:158-166)"
---

<objective>
Close the two narrow backend test gaps the research identified and confirm the existing coverage. TEST-01 (trade edge cases + P&L) is already fully covered in test_portfolio.py — this plan runs and asserts it rather than duplicating it. TEST-02 gets a new full-app integration module that exercises portfolio/history/watchlist GETs through the assembled create_app() (test_main.py previously only covered watchlist end-to-end). TEST-03 gets a new module covering "valid JSON, wrong schema" LLM responses (test_llm.py covered invalid JSON and network errors but not schema-mismatch).

Purpose: These are mechanical extensions of strong existing analogs (per 04-PATTERNS.md "exact" match). They make the backend test requirements verifiably complete without inventing new product behavior.
Output: backend/tests/test_main_api_coverage.py, backend/tests/test_llm_malformed.py.
</objective>

<execution_context>
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/workflows/execute-plan.md
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-testing/04-RESEARCH.md
@.planning/phases/04-docker-testing/04-PATTERNS.md
@backend/tests/test_main.py
@backend/tests/test_llm.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-app API coverage test (TEST-02 gap-fill) + confirm TEST-01</name>
  <files>backend/tests/test_main_api_coverage.py</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-PATTERNS.md — "backend/tests/test_main_api_coverage.py (test, integration-TestClient)" section (analog: test_main.py + test_chat_route.py:216-231), including the exact tmp_db fixture, create_app() TestClient context-manager pattern, and the 3-test plan
    - backend/tests/test_main.py (full file) — the tmp_db fixture (lines 25-36) and the watchlist full-app test (lines 92-114) to mirror
    - backend/tests/test_portfolio.py — confirm the TEST-01 edge-case tests already exist (insufficient cash, oversell, partial sell, weighted avg cost) so this plan only RUNS them, not re-creates
    - backend/app/routes/portfolio.py + backend/app/routes/watchlist.py — confirm GET /api/portfolio, GET /api/portfolio/history, GET /api/watchlist response shapes (cash_balance, positions, total_value; history list; 10 tickers)
  </read_first>
  <behavior>
    - Test: GET /api/portfolio on a fresh full app -> 200, cash_balance == 10000.0, positions == []
    - Test: GET /api/portfolio/history on a fresh full app -> 200, body is a list (empty or snapshot rows)
    - Test: GET /api/watchlist on a fresh full app -> 200, returns exactly 10 default tickers
  </behavior>
  <action>
    Create backend/tests/test_main_api_coverage.py mirroring backend/tests/test_main.py verbatim in conventions: from __future__ import annotations; import pytest and TestClient; define a tmp_db fixture that monkeypatches DB_PATH to tmp_path / "test_main_api_coverage.db" and sets SNAPSHOT_INTERVAL to "0.2" and STATIC_DIR to a nonexistent path (so the static mount is skipped, per test_chat_route.py:216-231). Each test imports create_app() INSIDE the test body, constructs the app, and uses TestClient as a context manager so lifespan startup/shutdown fires. Write three tests per the 04-PATTERNS plan: (1) GET /api/portfolio asserts 200 + cash_balance == 10000.0 + positions == []; (2) GET /api/portfolio/history asserts 200 + isinstance(body, list); (3) GET /api/watchlist asserts 200 + exactly 10 tickers. Do NOT duplicate the trade edge-case tests from test_portfolio.py — TEST-01 is satisfied there; the verify command for this task runs the full suite which includes them. Do NOT touch the real db/finally.db — the DB_PATH monkeypatch must be set before create_app import (Pitfall 8).
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally/backend" && uv run --extra dev pytest tests/test_main_api_coverage.py tests/test_portfolio.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_main_api_coverage.py exits 0 with 3 passing tests
    - test_main_api_coverage.py asserts cash_balance == 10000.0 for a fresh GET /api/portfolio
    - test_main_api_coverage.py asserts GET /api/watchlist returns exactly 10 tickers
    - test_main_api_coverage.py uses create_app() with TestClient as a context manager and monkeypatches DB_PATH + STATIC_DIR
    - The existing trade edge-case tests in test_portfolio.py still pass (TEST-01 confirmed, not re-created)
  </acceptance_criteria>
  <done>Full-app GET portfolio/history/watchlist round-trips pass through create_app(); TEST-01 edge cases confirmed green via the existing test_portfolio.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Malformed-LLM (wrong-schema) test (TEST-03 gap-fill)</name>
  <files>backend/tests/test_llm_malformed.py</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-PATTERNS.md — "backend/tests/test_llm_malformed.py (test, unit-async)" section (analog: test_llm.py), including the imports pattern, the async test pattern, the critical monkeypatch import-ordering, the fake litellm response shape, and the assertion contract (isinstance ChatResponse, trades == [], watchlist_changes == [], message truthy)
    - .planning/phases/04-docker-testing/04-RESEARCH.md — "Code Examples -> Backend test gap-fill example (TEST-03: malformed LLM response)" (RESEARCH lines 703-748) — the two ready-to-adapt tests
    - backend/tests/test_llm.py (lines 152-225) — the existing invalid-JSON and network-error tests to mirror conventions, and to confirm the gap (no valid-JSON-wrong-schema test)
    - backend/app/llm.py — ChatResponse schema and complete_chat graceful-fallback path
  </read_first>
  <behavior>
    - Test: LLM returns valid JSON missing the required message field ({"trades": []}) -> complete_chat returns a ChatResponse with a truthy fallback message and trades == []
    - Test: LLM returns valid JSON with a wrong-typed message ({"message": 12345, "trades": []}) -> complete_chat returns a ChatResponse with a truthy fallback message
  </behavior>
  <action>
    Create backend/tests/test_llm_malformed.py mirroring backend/tests/test_llm.py conventions exactly: from __future__ import annotations; import pytest; use @pytest.mark.asyncio for the async tests; in each test monkeypatch.delenv("LLM_MOCK", raising=False) and monkeypatch.setenv("OPENROUTER_API_KEY", "sk-test"), then import app.llm as llm_mod, monkeypatch llm_mod.completion to a lambda returning a fake response object whose choices[0].message.content is the malformed-but-valid JSON string, THEN import complete_chat and ChatResponse from app.llm (import-after-patch ordering, per the analog). Add the two tests from the RESEARCH code example: (1) content '{"trades": []}' (missing message) asserts isinstance(result, ChatResponse), result.message is truthy, result.trades == []; (2) content '{"message": 12345, "trades": []}' (wrong type) asserts isinstance(result, ChatResponse) and result.message truthy. Follow the assertion contract verbatim. Do NOT call the real LLM (no network). Do NOT modify app/llm.py — this tests existing graceful-fallback behavior only.
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally/backend" && uv run --extra dev pytest tests/test_llm_malformed.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_llm_malformed.py exits 0 with at least 2 passing tests
    - Each test asserts isinstance(result, ChatResponse) and a truthy fallback result.message
    - The missing-message test asserts result.trades == []
    - The test patches app.llm.completion and makes zero network calls
    - app/llm.py is unchanged (no production code edited)
  </acceptance_criteria>
  <done>Valid-JSON-wrong-schema LLM responses are proven to degrade gracefully to a ChatResponse fallback; TEST-03 gap closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider -> app.llm parser | The LLM returns untrusted text that is parsed into a typed ChatResponse |
| test DB_PATH -> real DB | A misconfigured test could write to the real db/finally.db |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-llm | Tampering | app.llm response parsing | high | mitigate | The TEST-03 gap-fill proves adversarial/malformed (valid-JSON-wrong-schema) LLM output degrades to a safe ChatResponse fallback with empty trades — no unvalidated action executes; mirrors the existing invalid-JSON coverage in test_llm.py |
| T-04-biz | Tampering | trade validation (TEST-01) | high | mitigate | The existing test_portfolio.py edge cases (insufficient cash, oversell) are run by this plan's verify command, proving balance/share checks reject invalid trades |
| T-04-07 | Tampering | test DB isolation | medium | mitigate | Both new test files monkeypatch DB_PATH to a tmp_path file before create_app/db import (Pitfall 8), so tests never touch the real DB and cannot pollute each other |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| backend/tests/test_main_api_coverage.py | new file | 3 full-app create_app() integration tests: GET portfolio, history, watchlist (TEST-02 gap-fill) |
| backend/tests/test_llm_malformed.py | new file | 2+ async tests for valid-JSON-wrong-schema graceful fallback (TEST-03 gap-fill) |
| (no new file) | confirmation | TEST-01 satisfied by existing backend/tests/test_portfolio.py — run, not re-created |
</artifacts>

<verification>
- cd backend && uv run --extra dev pytest tests/test_main_api_coverage.py tests/test_llm_malformed.py -q exits 0
- The full backend suite stays green: cd backend && uv run --extra dev pytest -q exits 0
- No production code under backend/app/ is modified
</verification>

<success_criteria>
The backend test requirements are verifiably complete: trade edge cases (existing) pass, full-app portfolio/history/watchlist GETs pass, and malformed-schema LLM responses degrade gracefully — all green under uv run --extra dev pytest.
</success_criteria>

<output>
Create .planning/phases/04-docker-testing/04-03-SUMMARY.md when done
</output>