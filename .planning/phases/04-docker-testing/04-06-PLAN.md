---
phase: 04-docker-testing
plan: 06
type: execute
wave: 3
depends_on: ["04-04", "04-05"]
files_modified:
  - test/e2e/01-fresh-start.spec.ts
  - test/e2e/02-buy.spec.ts
  - test/e2e/03-sell.spec.ts
  - test/e2e/04-chat.spec.ts
autonomous: true
requirements: [TEST-05, TEST-06, TEST-07, TEST-08]
user_setup: []

must_haves:
  truths:
    - "01-fresh-start.spec.ts asserts a fresh load shows $10,000 cash + total and exactly 10 watchlist rows, with the connection dot reaching streaming and at least one live price (handles the SSE first-batch delay via expect.poll)"
    - "02-buy.spec.ts buys 1 AAPL via the trade bar and asserts cash decreases and position-row-AAPL becomes visible"
    - "03-sell.spec.ts (after owning shares) sells via the trade bar and asserts cash increases and the position updates or disappears"
    - "04-chat.spec.ts stubs /api/chat with page.route to return a deterministic executed trade and asserts the assistant bubble plus the trade-chip text Bought 1 AAPL @ $150.00 and position-row-AAPL visible"
    - "All four specs pass under the compose runner: docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright exits 0"
  artifacts:
    - "test/e2e/01-fresh-start.spec.ts (TEST-05)"
    - "test/e2e/02-buy.spec.ts (TEST-06)"
    - "test/e2e/03-sell.spec.ts (TEST-07)"
    - "test/e2e/04-chat.spec.ts (TEST-08, page.route stub)"
  key_links:
    - "specs use the data-testid hooks from 04-04 (header-cash, header-total, watchlist-row, price, connection-dot, trade-ticker-input/qty-input/buy-button/sell-button, position-row-{ticker}, chat-input, chat-send, chat-message, trade-chip)"
    - "TEST-05 MUST tolerate the simulator first-batch delay (expect.poll with timeout, NOT an instant assertion) — Pitfall 4"
    - "TEST-08 MUST stub /api/chat via page.route (the default LLM_MOCK returns empty trades, so it cannot show a trade chip) — Pitfall 6; stub body shape is {message, actions:{trades:[...], watchlist_changes:[]}} matching backend handle_chat"
---

<objective>
Write the four Playwright E2E specs that prove the four success-criteria user behaviors against the running container: fresh start ($10k + 10 streaming tickers), buy (cash down, position appears), sell (cash up, position updates), and AI chat (deterministic stubbed trade confirmation). They run inside the wave-2 compose infra using the wave-1 frontend testid hooks.

Purpose: These specs are the executable acceptance of the phase's E2E requirements. Two are subtle: TEST-05 must absorb the simulator's first-SSE-batch delay, and TEST-08 must stub /api/chat (the default mock returns no trades, so it cannot render a trade chip).
Output: test/e2e/01-fresh-start.spec.ts, 02-buy.spec.ts, 03-sell.spec.ts, 04-chat.spec.ts.
</objective>

<execution_context>
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/workflows/execute-plan.md
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-testing/04-RESEARCH.md
@.planning/phases/04-docker-testing/04-PATTERNS.md
@.planning/phases/04-docker-testing/04-04-PLAN.md
@.planning/phases/04-docker-testing/04-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh-start, buy, and sell specs (TEST-05, TEST-06, TEST-07)</name>
  <files>test/e2e/01-fresh-start.spec.ts, test/e2e/02-buy.spec.ts, test/e2e/03-sell.spec.ts</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-RESEARCH.md — "Code Examples -> Playwright spec example (TEST-05: fresh start)" (RESEARCH lines 643-663); Pitfall 4 (SSE first-batch delay -> expect.poll); the 04-PATTERNS.md "No Analog Found" rows for 02-buy.spec.ts and 03-sell.spec.ts describing the buy/sell selector flow
    - .planning/phases/04-docker-testing/04-04-PLAN.md — the exact testids the specs target (header-cash, header-total, watchlist-row, price, connection-dot, trade-ticker-input, trade-qty-input, trade-buy-button, trade-sell-button, position-row-{ticker})
    - .planning/phases/04-docker-testing/04-05-PLAN.md — the baseURL is the compose service name (page.goto('/') resolves against http://app:8000); workers 1, single shared DB (so 03-sell can rely on state from earlier in its own spec)
  </read_first>
  <action>
    Create three specs importing { test, expect } from '@playwright/test'. 01-fresh-start.spec.ts (TEST-05): page.goto('/'); assert getByTestId('header-cash') contains $10,000 and getByTestId('header-total') contains $10,000; assert getByTestId('watchlist-row') has count 10; assert getByTestId('connection-dot') aria-label matches /streaming/i within a generous timeout; then use expect.poll over the count of [data-testid=watchlist-row] [data-testid=price] cells containing a $ to be >= 1 within ~10s (absorbs the simulator first-batch delay — Pitfall 4; do NOT assert prices instantly). 02-buy.spec.ts (TEST-06): goto('/'); read the current header-cash text; fill trade-ticker-input with AAPL, fill trade-qty-input with 1, click trade-buy-button; assert position-row-AAPL becomes visible and header-cash decreased from the captured value (poll/await the optimistic update). 03-sell.spec.ts (TEST-07): goto('/'); ensure an AAPL position exists by first buying 1 (buy then sell within the same spec so it is self-contained given the shared DB and worker=1); capture header-cash; fill trade-ticker-input AAPL, qty 1, click trade-sell-button; assert header-cash increased and position-row-AAPL either disappears or shows a reduced quantity. Make each spec self-contained (do not rely on cross-file ordering). Use the testids only (no CSS-class or text selectors for the load-bearing assertions).
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally" && node -e "for(const f of ['01-fresh-start','02-buy','03-sell']){const s=require('fs').readFileSync('test/e2e/'+f+'.spec.ts','utf8'); if(!/@playwright\/test/.test(s)||!/getByTestId/.test(s)) {console.error('FAIL '+f); process.exit(1);}} console.log('OK')" && grep -q 'expect.poll' test/e2e/01-fresh-start.spec.ts && grep -q 'trade-buy-button' test/e2e/02-buy.spec.ts && grep -q 'trade-sell-button' test/e2e/03-sell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three spec files import @playwright/test and use getByTestId (no CSS/text selectors for load-bearing assertions)
    - 01-fresh-start.spec.ts asserts $10,000 cash + total, 10 watchlist rows, connection dot streaming, and uses expect.poll for at least one live price (Pitfall 4)
    - 02-buy.spec.ts fills trade-ticker-input/qty-input, clicks trade-buy-button, asserts header-cash decreased and position-row-AAPL visible
    - 03-sell.spec.ts is self-contained (buys then sells), clicks trade-sell-button, asserts header-cash increased and the AAPL position updates/disappears
    - Under the compose runner these three specs pass (validated at the phase gate via the compose command)
  </acceptance_criteria>
  <done>Fresh-start, buy, and sell behaviors are proven end-to-end through the running container using the stable testids, tolerant of the SSE first-batch delay.</done>
</task>

<task type="auto">
  <name>Task 2: AI chat spec with deterministic stub (TEST-08)</name>
  <files>test/e2e/04-chat.spec.ts</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-RESEARCH.md — "Code Examples -> Playwright spec example (TEST-08: AI chat with deterministic stub)" (RESEARCH lines 665-700) and Pitfall 6 (default LLM_MOCK returns empty trades; MUST use page.route to inject a trade)
    - .planning/phases/04-docker-testing/04-04-PLAN.md — the chat testids (chat-input, chat-send, chat-message, trade-chip) and the confirmation that the trade-chip text is "Bought {qty} {ticker} @ ${price}"
    - backend/app/routes/chat.py + backend/app/chat_service.py (handle_chat return shape) — confirm the response body the stub must emit is {message, actions:{trades:[...], watchlist_changes:[]}} and each trade record has ticker, side, quantity, status:"executed", detail:"Executed at $150.00; ..." (the frontend TradeChip extracts the price from detail and renders Bought/Sold)
    - frontend/src/components/chat/ChatMessage.tsx — confirm TradeChip renders "Bought {quantity} {ticker} @ ${priceLabel}" so the asserted text matches
  </read_first>
  <action>
    Create test/e2e/04-chat.spec.ts importing { test, expect } from '@playwright/test'. Before navigating, register page.route('**/api/chat', ...) to fulfill with status 200, contentType application/json, and a body matching the backend handle_chat shape exactly: { message: '[MOCK] Buying 1 share of AAPL', actions: { trades: [{ ticker: 'AAPL', side: 'buy', quantity: 1, status: 'executed', detail: 'Executed at $150.00; cash_balance=9850.00' }], watchlist_changes: [] } } (Pitfall 6 — the default LLM_MOCK returns empty trades, so the spec MUST inject the trade via the route stub; do NOT add a product env var for this). Then page.goto('/'); wait for getByTestId('chat-input') to be enabled (chat is enabled when /api/health reports chat_enabled — under LLM_MOCK it is true) within a generous timeout; fill chat-input with "buy 1 AAPL"; click chat-send; assert the last [data-testid=chat-message] contains [MOCK]; assert getByTestId('trade-chip') (last) contains the text "Bought 1 AAPL @ $150.00" (matches the TradeChip render of the stub's detail price); assert getByTestId('position-row-AAPL') is visible (the buy went through the same trade path). Keep the stub body shape exactly aligned to the backend contract so the frontend renders the chip correctly. Use testids only for load-bearing assertions.
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally" && node -e "const s=require('fs').readFileSync('test/e2e/04-chat.spec.ts','utf8'); if(!/page\.route\(/.test(s)) {console.error('missing page.route stub'); process.exit(1);} if(!/actions/.test(s)||!/trades/.test(s)) {console.error('stub body must use actions.trades'); process.exit(1);} if(!/getByTestId\('trade-chip'\)|data-testid=trade-chip/.test(s)) {console.error('must assert trade-chip'); process.exit(1);} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 04-chat.spec.ts registers page.route('**/api/chat', ...) returning a deterministic executed-trade body
    - The stub body uses the backend shape {message, actions:{trades:[...], watchlist_changes:[]}} with a trade detail of "Executed at $150.00; ..."
    - The spec asserts the assistant chat-message contains [MOCK], the trade-chip contains "Bought 1 AAPL @ $150.00", and position-row-AAPL is visible
    - The spec does NOT rely on the default LLM_MOCK to produce a trade (Pitfall 6)
    - Under the compose runner this spec passes (phase-gate via compose command)
  </acceptance_criteria>
  <done>AI chat returns a response with an inline executed-trade confirmation, proven deterministically via a route stub that matches the backend response contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stubbed /api/chat -> frontend render | The route stub injects a response the frontend renders; it exercises the render path, not the real LLM |
| E2E browser -> app over compose net | The browser drives the app by service-name baseURL inside the compose network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-llm | Tampering | chat render path under stub | high | mitigate | TEST-08 stubs /api/chat at the network layer (page.route) instead of adding test-only product code or a real LLM key; the stub body matches the validated backend contract, so the test exercises the real frontend render of a trade confirmation without exposing a key or weakening validation |
| T-04-12 | Information Disclosure | E2E run credentials | medium | mitigate | All specs run against the LLM_MOCK=true app (compose, plan 04-05); no OPENROUTER_API_KEY is present in the E2E environment; the chat spec needs none because it stubs the endpoint |
| T-04-13 | Tampering | shared test DB state | low | mitigate | workers=1 + each spec self-contained (buy-before-sell) prevents cross-spec state races on the single shared finally-test-data volume |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| test/e2e/01-fresh-start.spec.ts | new file | TEST-05: $10k + 10 streaming tickers; expect.poll for first SSE batch |
| test/e2e/02-buy.spec.ts | new file | TEST-06: buy 1 AAPL -> cash down, position-row-AAPL visible |
| test/e2e/03-sell.spec.ts | new file | TEST-07: self-contained buy-then-sell -> cash up, position updates |
| test/e2e/04-chat.spec.ts | new file | TEST-08: page.route stub -> assistant reply + trade-chip "Bought 1 AAPL @ $150.00" |
</artifacts>

<verification>
- Each spec file imports @playwright/test and uses getByTestId for load-bearing assertions
- 01 uses expect.poll for live price; 02 uses trade-buy-button; 03 uses trade-sell-button; 04 uses page.route + asserts trade-chip
- Phase gate: docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright exits 0 (all four specs green)
</verification>

<success_criteria>
The four E2E specs encode the four phase success behaviors and pass against the running mock-LLM container: fresh start shows $10k and 10 streaming tickers, buy decreases cash and adds a position, sell increases cash, and AI chat shows an inline trade confirmation — all via stable testids and tolerant of SSE timing.
</success_criteria>

<output>
Create .planning/phases/04-docker-testing/04-06-SUMMARY.md when done
</output>