---
phase: 04-docker-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/layout/ConnectionDot.tsx
  - frontend/src/components/watchlist/WatchlistRow.tsx
  - frontend/src/components/trade/TradeBar.tsx
  - frontend/src/components/portfolio/PositionsTable.tsx
  - frontend/src/components/chat/ChatPanel.tsx
  - frontend/src/components/chat/ChatMessage.tsx
autonomous: true
requirements: [TEST-05, TEST-06, TEST-07, TEST-08]
user_setup: []

must_haves:
  truths:
    - "Header renders data-testid=header-total and data-testid=header-cash on the live value spans"
    - "ConnectionDot renders data-testid=connection-dot (its existing aria-label still encodes the streaming/reconnecting/lost status)"
    - "WatchlistRow renders data-testid=watchlist-row on the row and data-testid=price on the price cell"
    - "TradeBar renders data-testid on the ticker input, qty input, Buy button, and Sell button"
    - "PositionsTable renders a dynamic data-testid=position-row-{TICKER} on each row"
    - "ChatPanel renders data-testid=chat-input and data-testid=chat-send; ChatMessage renders data-testid=chat-message on each bubble and data-testid=trade-chip on executed-trade chips"
    - "The frontend still type-checks/builds after the edits (npm run build succeeds)"
  artifacts:
    - "7 edited frontend components carrying stable data-testid hooks for the Playwright E2E specs"
  key_links:
    - "These testids are the exact selectors the wave-3 E2E specs use: header-total, header-cash, connection-dot, watchlist-row, price, trade-ticker-input, trade-qty-input, trade-buy-button, trade-sell-button, position-row-{ticker}, chat-input, chat-send, chat-message, trade-chip"
    - "trade-chip text 'Bought {qty} {ticker} @ ${price}' is what TEST-08 asserts; the testid goes on the chip div whose text is already correct (no copy change)"
    - "additions are attribute-only — no behavior, styling, or copy changes (mechanical edits per 04-PATTERNS.md file:line targets)"
---

<objective>
Add stable data-testid hooks to seven existing frontend components so the wave-3 Playwright specs have copy-proof selectors. Every edit is a pure attribute addition at a precise file:line target documented in 04-PATTERNS.md — no behavior, styling, or copy changes. This plan unblocks all four E2E specs and is a hard prerequisite for them (without these hooks TEST-05..08 cannot pass).

Purpose: The frontend was built with aria-label and Tailwind classes but no data-testid (Pitfall 5). E2E selectors targeting CSS classes are flaky; data-testid survives copy/i18n changes. Doing this as one mechanical plan keeps the E2E specs reviewable on their own.
Output: 7 edited components.
</objective>

<execution_context>
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/workflows/execute-plan.md
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-testing/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layout + watchlist testids (Header, ConnectionDot, WatchlistRow)</name>
  <files>frontend/src/components/layout/Header.tsx, frontend/src/components/layout/ConnectionDot.tsx, frontend/src/components/watchlist/WatchlistRow.tsx</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-PATTERNS.md — "Frontend data-testid additions — concrete edit targets" -> the Header.tsx, ConnectionDot.tsx, and WatchlistRow.tsx subsections (exact line numbers and the JSX to target)
    - frontend/src/components/layout/Header.tsx (read the Total Value and Cash blocks ~lines 50-62)
    - frontend/src/components/layout/ConnectionDot.tsx (read the status span ~lines 39-44)
    - frontend/src/components/watchlist/WatchlistRow.tsx (read the row container ~lines 72-87 and the PriceFlash price element ~lines 93-99)
  </read_first>
  <action>
    Add data-testid attributes only, at the 04-PATTERNS.md targets. In Header.tsx: add data-testid="header-total" to the Total Value inner span (the one rendering formatCurrency(total)) and data-testid="header-cash" to the Cash inner span (the one rendering portfolio cash_balance). In ConnectionDot.tsx: add data-testid="connection-dot" to the existing status span that already has role="status" and the aria-label (leave the aria-label intact — it encodes the streaming/reconnecting/lost text the E2E matches). In WatchlistRow.tsx: add data-testid="watchlist-row" to the root row div (role="button") and add data-testid="price" to the PriceFlash element that renders the price text. Do NOT change any classNames, copy, handlers, or component logic — attribute additions only.
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally/frontend" && grep -q 'data-testid="header-total"' src/components/layout/Header.tsx && grep -q 'data-testid="header-cash"' src/components/layout/Header.tsx && grep -q 'data-testid="connection-dot"' src/components/layout/ConnectionDot.tsx && grep -q 'data-testid="watchlist-row"' src/components/watchlist/WatchlistRow.tsx && grep -q 'data-testid="price"' src/components/watchlist/WatchlistRow.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx contains data-testid="header-total" and data-testid="header-cash"
    - ConnectionDot.tsx contains data-testid="connection-dot" and still has its role="status" aria-label
    - WatchlistRow.tsx contains data-testid="watchlist-row" and data-testid="price"
    - No className, copy, or handler in these three files changed (attribute additions only)
  </acceptance_criteria>
  <done>Header, ConnectionDot, and WatchlistRow carry their E2E testids with no behavior/copy change.</done>
</task>

<task type="auto">
  <name>Task 2: Trade + positions testids (TradeBar, PositionsTable)</name>
  <files>frontend/src/components/trade/TradeBar.tsx, frontend/src/components/portfolio/PositionsTable.tsx</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-PATTERNS.md — the TradeBar.tsx subsection (ticker input ~127-135, qty input ~136-145, Buy button ~148-154, Sell button ~155-162) and the PositionsTable.tsx subsection (row tr ~99-121, dynamic position-row-{ticker})
    - frontend/src/components/trade/TradeBar.tsx (read the input + button JSX)
    - frontend/src/components/portfolio/PositionsTable.tsx (read the row tr that keys on position.ticker)
  </read_first>
  <action>
    Add data-testid attributes only, at the 04-PATTERNS.md targets. In TradeBar.tsx: add data-testid="trade-ticker-input" to the ticker text input, data-testid="trade-qty-input" to the quantity number input, data-testid="trade-buy-button" to the Buy submit button, and data-testid="trade-sell-button" to the Sell button. In PositionsTable.tsx: add a dynamic data-testid using a template literal of the form position-row-${position.ticker} to the row tr that already keys on position.ticker (so a position in AAPL yields data-testid="position-row-AAPL"). Do NOT change classNames, copy, handlers, the uppercase onChange, or the submit logic — attribute additions only.
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally/frontend" && grep -q 'data-testid="trade-ticker-input"' src/components/trade/TradeBar.tsx && grep -q 'data-testid="trade-qty-input"' src/components/trade/TradeBar.tsx && grep -q 'data-testid="trade-buy-button"' src/components/trade/TradeBar.tsx && grep -q 'data-testid="trade-sell-button"' src/components/trade/TradeBar.tsx && grep -q 'position-row-' src/components/portfolio/PositionsTable.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TradeBar.tsx contains data-testid="trade-ticker-input", data-testid="trade-qty-input", data-testid="trade-buy-button", data-testid="trade-sell-button"
    - PositionsTable.tsx contains a data-testid template literal yielding position-row-${ticker}
    - No className, copy, or handler in these two files changed (attribute additions only)
  </acceptance_criteria>
  <done>TradeBar inputs/buttons and PositionsTable rows carry their E2E testids (dynamic per ticker) with no behavior/copy change.</done>
</task>

<task type="auto">
  <name>Task 3: Chat testids (ChatPanel, ChatMessage) + build check</name>
  <files>frontend/src/components/chat/ChatPanel.tsx, frontend/src/components/chat/ChatMessage.tsx</files>
  <read_first>
    - .planning/phases/04-docker-testing/04-PATTERNS.md — the ChatPanel.tsx subsection (chat input ~225-233, send button ~234-240) and the ChatMessage.tsx subsection (outer bubble wrappers ~46-65 for both user and assistant branches; TradeChip outer div ~84-95)
    - frontend/src/components/chat/ChatPanel.tsx (read the input + send button JSX)
    - frontend/src/components/chat/ChatMessage.tsx (read both bubble branches and the TradeChip component)
    - .planning/phases/04-docker-testing/04-RESEARCH.md — "Playwright spec example (TEST-08)" to confirm trade-chip is targeted via getByTestId('trade-chip').last() and asserts text "Bought 1 AAPL @ $150.00"
  </read_first>
  <action>
    Add data-testid attributes only, at the 04-PATTERNS.md targets. In ChatPanel.tsx: add data-testid="chat-input" to the chat text input and data-testid="chat-send" to the Send submit button. In ChatMessage.tsx: add data-testid="chat-message" to the message bubble container in BOTH branches (user bubble div and assistant outer flex container) so any message is targetable and .last() resolves the latest; add data-testid="trade-chip" to the outer div of the executed-trade TradeChip (the div whose text renders the "Bought/Sold {qty} {ticker} @ ${price}" string). Do NOT change the chip text/copy (TEST-08 asserts the existing string), classNames, or handlers — attribute additions only. After all three tasks' edits, run the frontend build to confirm the static export still compiles.
  </action>
  <verify>
    <automated>cd "E:/work/GroupBWT/TestWork/Сourses/3/finally/frontend" && grep -q 'data-testid="chat-input"' src/components/chat/ChatPanel.tsx && grep -q 'data-testid="chat-send"' src/components/chat/ChatPanel.tsx && grep -q 'data-testid="chat-message"' src/components/chat/ChatMessage.tsx && grep -q 'data-testid="trade-chip"' src/components/chat/ChatMessage.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ChatPanel.tsx contains data-testid="chat-input" and data-testid="chat-send"
    - ChatMessage.tsx contains data-testid="chat-message" (on both bubble branches) and data-testid="trade-chip"
    - The TradeChip text "Bought/Sold {qty} {ticker} @ ${price}" is unchanged
    - npm run build (in frontend/) exits 0 — the static export still compiles after all testid edits
  </acceptance_criteria>
  <done>Chat input/send and message/trade-chip carry their E2E testids; the frontend still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test selectors -> rendered DOM | testids are an implicit contract between the specs and the components; drift breaks tests, not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | frontend DOM attributes | low | accept | data-testid attributes carry no auth or business meaning and are inert at runtime; pure test hooks. No security-relevant change; accepted |
| T-04-09 | Information Disclosure | testid naming | low | accept | testids name UI roles (header-cash, trade-buy-button) not secrets; no sensitive data exposed; accepted |
</threat_model>

<artifacts>
## Artifacts this phase produces (this plan)

| Artifact | Kind | Notes |
|----------|------|-------|
| frontend/src/components/layout/Header.tsx | modified | + data-testid header-total, header-cash |
| frontend/src/components/layout/ConnectionDot.tsx | modified | + data-testid connection-dot |
| frontend/src/components/watchlist/WatchlistRow.tsx | modified | + data-testid watchlist-row, price |
| frontend/src/components/trade/TradeBar.tsx | modified | + data-testid trade-ticker-input, trade-qty-input, trade-buy-button, trade-sell-button |
| frontend/src/components/portfolio/PositionsTable.tsx | modified | + dynamic data-testid position-row-{ticker} |
| frontend/src/components/chat/ChatPanel.tsx | modified | + data-testid chat-input, chat-send |
| frontend/src/components/chat/ChatMessage.tsx | modified | + data-testid chat-message (both branches), trade-chip |
</artifacts>

<verification>
- grep confirms every required data-testid is present in its target file
- No className/copy/handler changes (attribute-only edits)
- npm run build (frontend/) exits 0
</verification>

<success_criteria>
All 14 E2E selector hooks (header-total, header-cash, connection-dot, watchlist-row, price, trade-ticker-input, trade-qty-input, trade-buy-button, trade-sell-button, position-row-{ticker}, chat-input, chat-send, chat-message, trade-chip) exist on the right elements, with no behavior/copy change, and the frontend still compiles.
</success_criteria>

<output>
Create .planning/phases/04-docker-testing/04-04-SUMMARY.md when done
</output>