---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/db.py
  - backend/app/routes/__init__.py
  - backend/tests/test_db.py
  - .gitignore
  - db/.gitkeep
autonomous: false
requirements: [CORE-03]
user_setup: []

must_haves:
  truths:
    - "A fresh cold start with no existing database file auto-creates all 6 tables and seeds the default user ($10,000 cash) plus 10 default watchlist tickers, with no manual setup"
    - "Re-running init_db on an already-seeded database is idempotent (no duplicate users, no duplicate watchlist rows, cash unchanged)"
    - "All database access uses async aiosqlite with parameterized queries"
  artifacts:
    - "backend/app/db.py (connection helper, SCHEMA, init_db, seed, query helpers)"
    - "backend/tests/test_db.py"
    - "aiosqlite + httpx added to backend/pyproject.toml and backend/uv.lock"
  key_links:
    - "get_watchlist_tickers() returns the seeded ticker symbols that main.py will feed to MarketDataSource.start() so prices stream"
    - "DEFAULT_WATCHLIST tickers match app.market.seed_prices.SEED_PRICES keys so every seeded ticker has a live price"
---

<objective>
Establish the SQLite persistence layer for the FinAlly backend: add the async database driver, define the 6-table schema from planning/PLAN.md section 7, and implement lazy initialization that auto-creates tables and seeds default data on first use.

Purpose: Every downstream route (portfolio, watchlist) and the app lifecycle depend on a working, self-seeding database. This is the foundational Wave 1 plan.
Output: `backend/app/db.py` with connection + schema + lazy init + seed + query helpers, fully tested; `aiosqlite` and `httpx` dependencies added.
</objective>

<execution_context>
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/workflows/execute-plan.md
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@planning/PLAN.md
@backend/pyproject.toml
@backend/app/market/seed_prices.py
@backend/tests/conftest.py
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_produced>
New symbols and files created by this plan (for downstream source-grounding):
- File `backend/app/db.py` with: constant `SCHEMA` (str), `DEFAULT_USER_ID` ("default"), `DEFAULT_CASH` (10000.0), `DEFAULT_WATCHLIST` (list[str]); functions `get_db_path() -> str`, async `connect()` (async context manager yielding an aiosqlite.Connection with `row_factory = aiosqlite.Row`), async `init_db() -> None`, async `get_watchlist_tickers() -> list[str]`
- File `backend/tests/test_db.py`
- File `db/.gitkeep`
- File `backend/app/routes/__init__.py` (package marker scaffolding for the Wave 2 route plans)
- New dependency `aiosqlite` (runtime) and `httpx` (dev) in `backend/pyproject.toml` + `backend/uv.lock`
- New environment variable `DB_PATH` (default `db/finally.db`)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify legitimacy of new pip packages (aiosqlite, httpx)</name>
  <files>none (human verification gate before any install)</files>
  <what-built>This plan adds two new PyPI packages: `aiosqlite` (async SQLite driver, required because CLAUDE.md mandates async DB access in FastAPI handlers) and `httpx` (required by FastAPI's TestClient for route tests in later plans). No RESEARCH.md Package Legitimacy Audit exists, so both are treated as [ASSUMED] and must be human-verified before install.</what-built>
  <action>STOP before installing. This is a blocking package-legitimacy gate: no install task may run until a human approves. Present the two packages (aiosqlite, httpx) for verification per how-to-verify below, then wait for the resume signal. This gate is never auto-approvable.</action>
  <how-to-verify>
    1. Open https://pypi.org/project/aiosqlite/ — confirm it is the official async SQLite library (maintained, millions of downloads, repo github.com/omnilib/aiosqlite).
    2. Open https://pypi.org/project/httpx/ — confirm it is the official Encode HTTPX client (maintained by encode, repo github.com/encode/httpx).
    3. Confirm the exact names `aiosqlite` and `httpx` (no typo-squat variants).
  </how-to-verify>
  <verify><human-check>Both packages confirmed legitimate on pypi.org with the expected official repositories and exact names.</human-check></verify>
  <resume-signal>Type "approved" to allow installing aiosqlite and httpx, or describe a concern.</resume-signal>
  <done>A human has confirmed aiosqlite and httpx are the official, non-typo-squatted packages and approved the install.</done>
</task>

<task type="auto">
  <name>Task 2: Add async DB driver dependency and repo hygiene</name>
  <read_first>
    - backend/pyproject.toml (current dependency list and dev extras)
    - .gitignore (current ignore rules)
    - planning/PLAN.md (section 4 Directory Structure and section 7 Database — confirms db/finally.db is the runtime SQLite path and is gitignored)
  </read_first>
  <files>backend/pyproject.toml, backend/uv.lock, .gitignore, db/.gitkeep, backend/app/routes/__init__.py</files>
  <action>
    In `backend/pyproject.toml`, add `aiosqlite>=0.20.0` to the `[project]` `dependencies` array (runtime). Add `httpx>=0.27.0` to the `[project.optional-dependencies]` `dev` array (test client dependency). Then run `cd backend && uv sync --extra dev` to resolve and update `backend/uv.lock` so the lockfile pins both packages.
    Create `db/.gitkeep` (empty file) at the project root so the runtime volume-mount directory exists in the repo.
    Create `backend/app/routes/__init__.py` as an empty package marker (single module docstring line) so the Wave 2 portfolio and watchlist route plans can add modules under app/routes/ without a package-marker conflict.
    In `.gitignore`, append two ignore rules so the runtime SQLite database is never committed: a rule for `db/finally.db` and a rule for `db/*.db-journal` (and `db/*.db-wal`). Keep all existing `.gitignore` content intact.
  </action>
  <verify>
    <automated>cd backend && uv run python -c "import aiosqlite, httpx; print(aiosqlite.__name__, httpx.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` lists `aiosqlite>=0.20.0` under `[project].dependencies`
    - `backend/pyproject.toml` lists `httpx>=0.27.0` under `[project.optional-dependencies].dev`
    - `backend/uv.lock` contains an entry for `aiosqlite` and for `httpx`
    - `python -c "import aiosqlite, httpx"` succeeds inside the backend uv environment
    - `db/.gitkeep` exists
    - `backend/app/routes/__init__.py` exists
    - `.gitignore` contains an ignore rule matching `db/finally.db`
  </acceptance_criteria>
  <done>aiosqlite and httpx are installed and importable in the backend environment; db/ runtime directory is tracked via .gitkeep; the SQLite database file path is gitignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the SQLite schema, lazy init, and seed module</name>
  <read_first>
    - planning/PLAN.md (section 7 Database — exact table names, columns, types, defaults, UNIQUE constraints, and seed data)
    - backend/app/market/seed_prices.py (SEED_PRICES dict — DEFAULT_WATCHLIST must equal its keys)
    - backend/app/market/cache.py (read-only: shows the async/typed style and module structure to mirror)
    - backend/tests/conftest.py (async test fixtures available)
    - .planning/codebase/CONVENTIONS.md (module docstring, import order, type hints, snake_case, logging via logging.getLogger(__name__))
  </read_first>
  <files>backend/app/db.py, backend/tests/test_db.py</files>
  <behavior>
    - init_db() on a brand-new (missing) database file creates exactly 6 tables: users_profile, watchlist, positions, trades, portfolio_snapshots, chat_messages
    - After init_db(), users_profile has one row id="default" with cash_balance=10000.0
    - After init_db(), watchlist has exactly 10 rows, one per ticker AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX
    - Calling init_db() a second time does NOT create duplicate users or duplicate watchlist rows and does NOT reset cash_balance
    - get_watchlist_tickers() returns the 10 seeded ticker symbols as a list[str]
    - get_db_path() honors the DB_PATH env var and defaults to db/finally.db
  </behavior>
  <action>
    Create `backend/app/db.py` following CONVENTIONS.md (module docstring describing public API, `from __future__ import annotations`, sorted imports, type hints on every function, module-level `logger = logging.getLogger(__name__)`).
    Define module constants: `DEFAULT_USER_ID = "default"`, `DEFAULT_CASH = 10000.0`, and `DEFAULT_WATCHLIST: list[str]` equal to the 10 tickers AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX (these MUST match the keys of app.market.seed_prices.SEED_PRICES so each seeded ticker streams a live price). Add a comment noting this coupling.
    Define `SCHEMA` as a SQL string with `CREATE TABLE IF NOT EXISTS` for all 6 tables exactly per planning/PLAN.md section 7: users_profile(id TEXT PRIMARY KEY, cash_balance REAL, created_at TEXT); watchlist(id TEXT PRIMARY KEY, user_id TEXT, ticker TEXT, added_at TEXT, UNIQUE(user_id, ticker)); positions(id TEXT PRIMARY KEY, user_id TEXT, ticker TEXT, quantity REAL, avg_cost REAL, updated_at TEXT, UNIQUE(user_id, ticker)); trades(id TEXT PRIMARY KEY, user_id TEXT, ticker TEXT, side TEXT, quantity REAL, price REAL, executed_at TEXT); portfolio_snapshots(id TEXT PRIMARY KEY, user_id TEXT, total_value REAL, recorded_at TEXT); chat_messages(id TEXT PRIMARY KEY, user_id TEXT, role TEXT, content TEXT, actions TEXT, created_at TEXT).
    Implement `get_db_path() -> str`: read env `DB_PATH`, default `"db/finally.db"`; ensure the parent directory exists via os.makedirs(parent, exist_ok=True) before returning.
    Implement async `connect()` as an `@contextlib.asynccontextmanager` that opens `aiosqlite.connect(get_db_path())`, sets `db.row_factory = aiosqlite.Row`, executes `PRAGMA foreign_keys = ON`, yields the connection, and closes it on exit.
    Implement async `init_db() -> None`: open a connection, `executescript(SCHEMA)`, commit; then seed idempotently — check whether users_profile has a row with id=DEFAULT_USER_ID; if not, insert it with cash_balance=DEFAULT_CASH and created_at set to an ISO-8601 UTC timestamp; for each ticker in DEFAULT_WATCHLIST insert a watchlist row using uuid4 hex id, user_id=DEFAULT_USER_ID, added_at ISO timestamp, using `INSERT OR IGNORE` so the UNIQUE(user_id, ticker) constraint makes re-seeding a no-op; commit. Log seed counts at info level. Use only parameterized queries (`?` placeholders) — never string-formatted SQL values.
    Implement async `get_watchlist_tickers() -> list[str]`: SELECT ticker FROM watchlist WHERE user_id=DEFAULT_USER_ID ORDER BY added_at; return the list of ticker strings.
    Create `backend/tests/test_db.py` mirroring the behavior block. Use a per-test temp DB by setting the DB_PATH env var (via monkeypatch / patch.dict) to a tempfile path so tests never touch the real db/finally.db; cover: fresh init creates 6 tables (query sqlite_master), seeds 1 user with cash 10000.0, seeds 10 watchlist rows; second init_db() leaves counts unchanged and cash unchanged; get_watchlist_tickers() returns the 10 symbols.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_db.py -v && uv run --extra dev ruff check app/db.py tests/test_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run --extra dev pytest tests/test_db.py -v` passes all tests
    - A query of sqlite_master after init_db() returns the 6 table names: users_profile, watchlist, positions, trades, portfolio_snapshots, chat_messages
    - users_profile contains exactly one row with id="default" and cash_balance == 10000.0 after init_db()
    - watchlist contains exactly 10 rows after init_db(), and a second init_db() leaves it at 10 rows
    - get_watchlist_tickers() returns ["AAPL","GOOGL","MSFT","AMZN","TSLA","NVDA","META","JPM","V","NFLX"] (order by added_at)
    - `ruff check app/db.py tests/test_db.py` reports no errors
    - All SQL value insertion uses `?` parameter placeholders (no f-string/format SQL)
  </acceptance_criteria>
  <done>backend/app/db.py lazily creates the full 6-table schema and seeds the default user and watchlist idempotently; get_watchlist_tickers exposes the seeded symbols; tests prove cold-start creation, seed correctness, and idempotency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency supply chain → backend env | New PyPI packages (aiosqlite, httpx) introduced into the build |
| application code → SQLite file | Values written to the database during seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs (aiosqlite, httpx) | high | mitigate | Task 1 blocking-human legitimacy checkpoint verifies both packages on pypi.org before install; pinned in uv.lock |
| T-01-01 | Tampering | init_db / seed SQL | medium | mitigate | All inserts/queries use parameterized `?` placeholders; no string-formatted SQL values |
| T-01-02 | Information Disclosure | db/finally.db committed to repo | low | mitigate | .gitignore rule for db/finally.db prevents committing the runtime database |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest tests/test_db.py -v` — all DB tests pass
- `cd backend && uv run --extra dev ruff check app/ tests/` — lint clean
- `cd backend && uv run python -c "import aiosqlite, httpx"` — new deps importable
</verification>

<success_criteria>
A fresh database file is created on first init_db() with all 6 tables, the default $10,000 user, and 10 seeded watchlist tickers; re-initialization is idempotent; all DB access is async and parameterized.
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md` when done.
</output>
