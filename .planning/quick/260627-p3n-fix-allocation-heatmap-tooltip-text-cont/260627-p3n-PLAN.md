---
phase: quick-260627-p3n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/portfolio/PortfolioHeatmap.tsx
autonomous: true
requirements: [QUICK-260627-p3n]

must_haves:
  truths:
    - "Allocation heatmap tooltip item text renders in light foreground (#e6edf3), readable on the dark #161b22 tooltip background"
    - "Static export in frontend/out/ is rebuilt after the source edit so the browser receives the fixed JS, not stale chunks"
  artifacts:
    - frontend/src/components/portfolio/PortfolioHeatmap.tsx
    - frontend/out/
  key_links:
    - "<Tooltip itemStyle> prop → Recharts recharts-tooltip-item <li> text color (the prop contentStyle does NOT reach the item text)"
    - "source edit committed BEFORE rebuild → frontend/out/ reflects the change"
---

<objective>
Fix the Allocation heatmap tooltip text contrast. The Recharts `<Tooltip>` in
`PortfolioHeatmap.tsx` renders its inner `recharts-tooltip-item` text with the
library default black color, unreadable on the dark `#161b22` tooltip
background. The existing `contentStyle.color` only styles the outer container,
not the item text. Add `itemStyle={{ color: '#e6edf3' }}` to the `<Tooltip>` so
item text uses the light foreground color, then rebuild the static export.

Purpose: Tooltip values become legible, matching the dark-terminal theme.
Output: Edited `PortfolioHeatmap.tsx` (committed) + rebuilt `frontend/out/`
static export (gitignored working-tree artifact, not committed).
</objective>

<execution_context>
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/workflows/execute-plan.md
@E:/work/GroupBWT/TestWork/Сourses/3/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@frontend/src/components/portfolio/PortfolioHeatmap.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add itemStyle to the heatmap Tooltip</name>
  <files>frontend/src/components/portfolio/PortfolioHeatmap.tsx</files>
  <action>
In the existing `<Tooltip>` element (around lines 147-163), add a new prop
`itemStyle={{ color: '#e6edf3' }}` alongside the existing `contentStyle` and
`formatter` props. This sets the color on the Recharts
`recharts-tooltip-item` text, which `contentStyle` does not reach. Place the
`itemStyle` prop on the `<Tooltip>` element (e.g. immediately after the closing
brace of `contentStyle`, before `formatter`). Do NOT modify `contentStyle`, the
`formatter`, the `<Treemap>`, cell fills, `TreemapContent`, or any other code —
this is the only change (project rule: no unrequested changes, no refactoring).
  </action>
  <verify>
    <automated>grep -nF "itemStyle={{ color: '#e6edf3' }}" frontend/src/components/portfolio/PortfolioHeatmap.tsx</automated>
  </verify>
  <done>The single line `itemStyle={{ color: '#e6edf3' }}` is present on the `<Tooltip>` element; no other lines in the file changed except the inserted prop.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the static export (after the edit)</name>
  <files>frontend/out/</files>
  <action>
Run a clean rebuild AFTER Task 1's source edit so the static export reflects the
fix and contains no stale chunks. From the `frontend/` directory run:
`rm -rf out .next && npm run build`. The Next config uses `output: 'export'`,
so the build emits the static site to `frontend/out/`. `frontend/out/` and
`.next` are gitignored (confirmed via `git check-ignore`) — they are
working-tree artifacts, NOT committed. Do NOT force-add ignored build output.
Only `PortfolioHeatmap.tsx` is committed.
  </action>
  <verify>
    <automated>cd frontend && rm -rf out .next && npm run build</automated>
  </verify>
  <done>`npm run build` exits 0; `frontend/out/` is regenerated. No build artifacts staged for commit (out/ and .next remain gitignored).</done>
</task>

</tasks>

<verification>
- `grep -nF "itemStyle={{ color: '#e6edf3' }}" frontend/src/components/portfolio/PortfolioHeatmap.tsx` matches exactly one line.
- `cd frontend && npm run build` exits 0 and regenerates `frontend/out/`.
- `git status` shows only `frontend/src/components/portfolio/PortfolioHeatmap.tsx` as a tracked change (no `frontend/out/` or `.next` staged).
</verification>

<success_criteria>
- Tooltip item text in the Allocation heatmap renders light (#e6edf3), readable on the dark tooltip background.
- Source edit is the only committed change; rebuilt static export exists in the working tree but is not committed (gitignored).
- Build order honored: edit first, rebuild second.
</success_criteria>

<output>
Create `.planning/quick/260627-p3n-fix-allocation-heatmap-tooltip-text-cont/260627-p3n-SUMMARY.md` when done.
</output>
